[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'No build needed for Python API'"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
