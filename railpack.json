{
  "version": "1.0",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["npm install --prefix packages/shared && npm install --prefix apps/web && npm install"]
    },
    "build": {
      "commands": ["npm run build --workspace=@purple/shared && npm run build --workspace=@purple/web"]
    }
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@purple/web",
    "restartPolicyType": "onFailure"
  }
}