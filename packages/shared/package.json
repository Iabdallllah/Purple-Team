{
  "name": "@purple/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/ --ext .ts,.tsx --no-error-on-unmatched-pattern || echo \"lint: no config\"",
    "typecheck": "tsc --noEmit --skipLibCheck",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "zod": "^3.23.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.14.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}